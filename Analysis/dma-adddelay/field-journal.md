# Field Journal

日期：2026-09-11。

本轮记录范围为文档核对、此前只读结果整理和临时工程改动撤回。主机效果复现及功能集成未完成。

1. 原始文档来自不同机器上的历史进程内实验，当前复制目录只有 Markdown。引用路径存在不代表引用文件存在；本轮逐项检查四个运行日志，结果均缺失。
2. 多轮调查文档会撤销早期根因。用文件发布日期、具体实验结果和后续限定解释旧标题中的“权威”“成功”，不将其直接继承为当前结论。
3. 独立 DMA 探针最初缺少 DLL 搜索路径，退出码为加载依赖失败；修正当前进程 PATH 后才能取得输出。另一个探针仅查找 Super 模块，普通模块环境下返回空模块并非 DMA 连接失败。
4. 当前工具 manifest 要求提升，启动失败发生在服务运行前。零长度 stdout/stderr 不能当成连接成功记录。
5. 在复现成功前提前集成是本轮执行偏差，已撤回相关新增内容并重新构建。备份校验比“编译成功”更适合证明源码恢复。
6. 前后指令字节、函数执行结果、客户端字段状态以及服务器结果是不同层次的证据；某一层成功不能代替另一层。

复核文件：`evidence/final-verification.json`；详细报告：`writeups/document-review-and-restoration.md`。
