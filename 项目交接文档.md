# UnityExplorer-main 项目交接文档

## 1. 当前目的

本文件交接两个相邻但职责不同的项目：

| 项目 | 绝对路径 | 职责 |
| --- | --- | --- |
| Unity 外部分析器 | `J:\Code\C++\dma\er_new\UnityExplorer-main` | 基于 `er2` 的 Unity/IL2CPP 通用分析工具。提供 ImGui 浏览器、Headless CLI、GOM/MSID/Metadata/类型字段/方法和 Naraka 专用只读 Probe。 |
| Naraka 主项目 | `J:\Code\C++\dma\er_new\YJWJ_DMA_NEW` | 实际运行的 Naraka DMA 项目。维护当前版本的偏移、业务读取缓存、显示逻辑、输入与其他功能。 |

两者都面向 `NarakaBladepoint.exe` 的外部 DMA 内存读取，但**不是同一个 Visual Studio 工程，也没有自动同步代码或偏移**。UnityExplorer 应作为结构发现、只读验证和证据导出的分析侧；`YJWJ_DMA_NEW` 是经过验证后才承接偏移和读取链的运行侧。

## 2. UnityExplorer-main 的组成

### 2.1 核心库

- 统一入口：[Resolve202x.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\Resolve202x.hpp)
- Header-only 库根目录：`J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2`
- 初始化门面：[include\er2\unity2\init.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\init.hpp)
- 内存抽象：`er2::IMemoryAccessor`。算法层通过该接口读取，不应直接依赖某一种进程访问方式。
- DMA Accessor：[include\er2\os\dma\dma_memory_accessor.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\os\dma\dma_memory_accessor.hpp)

可用的 Unity 分析能力包括：

- 自动或手动定位 Unity 运行时模块；
- GOM 扫描与 `GameObject` 枚举；
- MSID 全局对象注册表扫描；
- `Transform` 坐标、主相机矩阵和 World-to-Screen；
- IL2CPP class、field、method 和 metadata 读取/导出；
- Naraka 的 `BuffManager`、`RuntimePropertyData`、落地状态和 `ActorModel` 容器 Probe。

### 2.2 两个可执行前端

| 前端 | 解决方案/入口 | 用途 |
| --- | --- | --- |
| ImGui 分析器 | [App\UnityExplorer.sln](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer.sln) / [App\UnityExplorer\main.cpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\main.cpp) | 交互式分析。连接 DMA 后可从界面查看模块、GOM、MSID、对象、类型和 Naraka Probe。 |
| Console / Headless | [App\ExternalResolveConsole.sln](J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole.sln) / [App\ExternalResolveConsole\main.cpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole\main.cpp) | 批处理、可复现的只读分析。输出 JSON、日志、CSV、runtime `dump.cs` 和 Offset 风格头文件。 |

`UnityExplorer` 项目的 Release 输出目录由工程设置为：

```text
J:\Code\C++\dma\er_new\UnityExplorer-main\bin\Release\UnityExplorer.exe
```

`ExternalResolveConsole` 没有在 `.vcxproj` 中重定义输出目录，Visual Studio 默认会输出到其配置目录（通常是 `App\ExternalResolveConsole\x64\Release` 或 `x64\Debug`）。应以 VS Build Output 实际报告的路径为准。

### 2.3 DMA 初始化路径

[App\UnityExplorer\MetickAdapter.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\MetickAdapter.hpp) 是本项目的 Naraka DMA 适配层，实际步骤为：

```text
MetickAdapter::Initialize("NarakaBladepoint.exe")
  -> mem::SetTargetProcessName(...)
  -> mem::DMA_Init()
  -> mem::Get_Process_Id(...)
  -> DmaData::PID / VMM handle

MetickAdapter::FixCr3()
  -> mem::FixCr32()

er2::g_ctx.memory = MetickAdapter
er2::g_ctx.pid = target PID
```

GUI 的入口是 `InitDmaConnection()`，位于 [App\UnityExplorer\main.cpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\main.cpp)。Headless 的入口是 `InitHeadlessDma()`，位于 [App\ExternalResolveConsole\main.cpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole\main.cpp)。两者都会：

1. 连接 MemProcFS/FPGA；
2. 尝试修复 CR3/DTB；
3. 枚举目标进程模块；
4. 设置 `er2::g_ctx` 的 PID、Accessor、`UnityPlayer` 与 `GameAssembly` 基址；
5. 读取 DOS/PE Header 验证 DMA 读取链。

运行目录需要包含 MemProcFS 运行时 DLL，至少是 `vmm.dll`、`leechcore.dll`、`FTD3XX.dll`。当前已有一套 Release DLL：

```text
J:\Code\C++\dma\er_new\UnityExplorer-main\bin\Release\vmm.dll
J:\Code\C++\dma\er_new\UnityExplorer-main\bin\Release\leechcore.dll
J:\Code\C++\dma\er_new\UnityExplorer-main\bin\Release\FTD3XX.dll
```

## 3. 使用方式

### 3.1 GUI 分析流程

1. 使用 Visual Studio 打开：

```text
J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer.sln
```

也可使用现有 IDE：

```powershell
& "J:\vs2026\Common7\IDE\devenv.exe" "J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer.sln"
```

2. 选择 `Release | x64` 后生成。注意该 `.sln` 的 `Debug|x64` 映射到 Release 配置，因此调试前必须确认 VS 当前实际生成的配置。
3. 启动 `UnityExplorer.exe`，目标进程填写 `NarakaBladepoint.exe`，运行模式选择 `DMA`。
4. 点击连接，先确认日志依次出现：连接 DMA、CR3 结果、已附加 PID、已发现 UnityPlayer、已发现 GameAssembly、DOS Header 正常。
5. 再按分析目的使用相应面板：
   - 通用对象/场景结构：GOM、MSID、GameObject/Component；
   - 相机/坐标：Camera、Transform、W2S；
   - 类型和字段：Class Inspector、Metadata 和 Runtime Dump；
   - Naraka 状态：Naraka BuffManager、RuntimeProperty、Ground、Actor Container Probe。

当前模块匹配实现位于 [include\er2\unity2\init\module_match.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\init\module_match.hpp)：

```text
UnityPlayer：名称以 UnityPlayer 开头并以 .dll 结尾
GameAssembly：名称以 GameAssembly 开头并以 .dll 结尾
```

因此 `UnityPlayer_LVB.dll`、`GameAssembly_Super.dll` 和 `GameAssembly_Super_IBT.dll` 都可被自动识别，不能把它们强行当作精确的 `UnityPlayer.dll` 或 `GameAssembly.dll`。

### 3.2 Headless 只读分析流程

先在 VS 生成 `ExternalResolveConsole`，然后在该 exe 的输出目录运行。最小只读基线命令：

```powershell
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\baseline.json" --log "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\baseline.log"
```

常用的 Naraka 分析命令示例：

```powershell
# 重新扫描当前 GameAssembly 内可能的 Manager RVA，输出 JSON 证据。
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --scan-managers --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\manager_scan.json" --log "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\manager_scan.log"

# 读取 BuffManager 结构与本地 Buff 容器。
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --naraka-buff-probe --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\buff_probe.json"

# 对 RuntimePropertyData 连续采样，生成差异和完整 CSV。
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --naraka-runtime-property-probe --naraka-runtime-property-samples 20 --naraka-runtime-property-interval-ms 100 --naraka-runtime-property-csv "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\runtime_property.csv" --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\runtime_property.json"

# 验证本地角色碰撞/落地相关结构。
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --naraka-ground-probe --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\ground.json"

# 扫描指定或自动选择的 ActorModel 容器。
.\ExternalResolveConsole.exe --headless --target NarakaBladepoint.exe --mode dma --scan none --naraka-actor-container-probe --out "J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output\actor_containers.json"
```

首次使用前手工创建输出目录：

```text
J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Output
```

Headless 的全部参数由 [include\er2\unity2\headless\probe_cli.hpp](J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\headless\probe_cli.hpp) 解析，帮助文本由 [App\ExternalResolveConsole\main.cpp](J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole\main.cpp) 输出。

## 4. 与 YJWJ_DMA_NEW 的关系

### 4.1 可以复用的部分

两个项目共享以下事实和工作流：

| 主题 | UnityExplorer-main | YJWJ_DMA_NEW |
| --- | --- | --- |
| 目标 | `NarakaBladepoint.exe` | `NarakaBladepoint.exe` |
| 运行时 | Unity IL2CPP、`UnityPlayer*`、`GameAssembly*` | Unity IL2CPP、`UnityPlayer*`、`GameAssembly*` |
| DMA 基础 | MemProcFS/VMMDLL，`MetickAdapter` 最终调用 `deps\Memory` | `mem` / VMMDLL 封装 |
| 发现手段 | GOM、MSID、metadata、class/field/method、Probe | dump、指定 pointer chain、Scatter runtime 读取 |
| 正确迁移方向 | 发现并导出可复现证据 | 审核证据后写入实际读取链与缓存线程 |

### 4.2 不能直接混用的部分

1. `UnityExplorer-main` 的 [App\UnityExplorer\GameOffsets.h](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\GameOffsets.h) 是分析器内的历史样例偏移，不是当前生产偏移来源。它目前的 `m_CharacterManager = 0x33CEEF0` 等值与主项目当前 Super 版本值不同。
2. `UnityExplorer-main\deps\Naraka` 是拷贝进分析项目的兼容/参考代码，不是主项目的实时工作副本。它不自动随着 `YJWJ_DMA_NEW` 更新。
3. `ExternalResolveConsole\main.cpp` 中 `kLegacyRvaCandidates` 的注释虽然称来自主项目，但表内 RVA 是旧快照，只能作为候选进行 readback 验证，不能直接写回生产代码。
4. `YJWJ_DMA_NEW` 使用自己的 `mem`、缓存结构和线程模型；UnityExplorer 使用 `er2::IMemoryAccessor` 与 `er2::g_ctx`。迁移时应迁移“已验证的 RVA/字段偏移/指针链”，不要直接复制调用代码。
5. 游戏会按版本和 DLL 变体加载不同模块：普通 `GameAssembly.dll`、`GameAssembly_Super.dll`、`GameAssembly_Super_IBT.dll` 的 RVA 不可互换。

### 4.3 当前生产偏移的权威位置

当前应优先阅读：

```text
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\Offset.h
```

该文件通过 `Offset::ApplyGameAssemblyOffsets()` 在普通 DLL 和 Super/SuperIBT 的两套偏移间切换。任何 UnityExplorer 的分析结论只有在满足下列条件后，才可更新这里：

1. 当前运行时 DLL 名称已由模块枚举确认；
2. `GameAssembly + RVA` 读到可读的 TypeInfo/Class 指针；
3. class 名称与预期类名相符；
4. static field/instance 链可以读到语义合理且可重复验证的对象；
5. 读取链经过实战状态验证，而不仅是登录界面验证；
6. 在 `Offset.h` 同时写明 DLL 变体、验证日期、dump 文件与验证方法。

## 5. 最近已验证的 CharacterManager 结论

验证日期：`2026-09-03`。对应 dump：

```text
J:\Code\C++\dma\er_new\dump\dump2026.09.03\dump2026.09.03.cs
```

实际加载模块：

```text
GameAssembly_Super.dll
```

在当时运行中的目标进程中，DMA 读取结果为：

```text
m_CharacterManager2 RVA = 0x374C2B8
direct static + 0x8 = 0x300000003
direct instance class = 0x0
direct chain valid = false

m_TransparentTagHandler2 RVA = 0x376ABF8
TransparentTagHandler.s_instance = valid
TransparentTagHandler.refCharacterMgr (+0xD8) = valid CharacterManager
CharacterManager + 0x18 = valid local ActorModel
CharacterManager + 0x50 -> +0x18 = valid alive-character list
```

因此 `YJWJ_DMA_NEW` 的 [Naraka\SDK.cpp](J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\SDK.cpp) 已修正 `CharacterManager::GetCharacterManagerPtr()`：

```text
优先：CharacterManager TypeInfo -> static + 0x8（仅在实例和 class 有效时使用）
回退：TransparentTagHandler TypeInfo -> static + 0x8 -> refCharacterMgr (+0xD8)
```

旧实现错误读取了 `TransparentTagHandler + 0xA8`。最新 dump 中该字段为 `alphaParam`，不是指针，导致错误值覆盖 `CharacterManagerPtr`，下游人物 ID、队伍、武器、坐标和精力全部显示为零。当前修复已由主项目运行验证恢复正常。

这条结论也说明一个重要规则：**TypeInfo 的 static field 布局和 Manager 的实例取得方式可能在同一次版本更新中变化，不能假设所有 Manager 都是 `class + 0xB8 -> static + 0x8`。**

## 6. 建议的跨项目更新流程

### Phase A：分析器中只读发现

1. 记录目标版本、进入的游戏状态、实际模块名和模块基址。
2. 使用 UnityExplorer GUI 或 Headless Probe 得到 JSON/CSV/log 证据。
3. 根据 runtime `dump.cs`、Metadata 或方法 RIP 引用确定 TypeInfo RVA、字段偏移与对象链。
4. 分别验证登录界面和实际对局。登录界面中局内单例、CharacterManager、BuffManager、本地 Actor 等对象为空是可能的生命周期状态，不能直接判为偏移错误。
5. 全部分析阶段保持只读；导出日志使用带日期的独立文件，避免覆盖之前的证据。

### Phase B：主项目最小迁移

1. 在 `YJWJ_DMA_NEW\Naraka\Offset.h` 添加或更新对应 DLL 变体的 RVA 和字段偏移。
2. 在 `YJWJ_DMA_NEW\Naraka\SDK.cpp` 或明确的缓存线程中添加最短读取链。
3. 为每一层指针做 null/readability 校验。链路失败不能留下上一局的对象地址。
4. 高频字段使用现有 Scatter 批读；不常变化的字典、容器、对象节点放入 `Cache_Player_World_Data` 等缓存路径。
5. 仅当读取链稳定且性能可接受，再接入显示、自瞄或其他业务线程。

### Phase C：运行时验收

1. 编译 `YJWJ_DMA_NEW` 后进入实际对局；
2. 输出必要但限频的链路诊断：模块变体、RVA、每层地址、失败层级、对象 class；
3. 对照 UnityExplorer 的 JSON/CSV 结果；
4. 测试换局、返回大厅和重新进入对局，确认缓存指针会刷新；
5. 验收完成后关闭调试日志，保留一份带日期的最终验证记录。

## 7. 重要文件索引

### UnityExplorer-main

```text
J:\Code\C++\dma\er_new\UnityExplorer-main\AGENTS.md
J:\Code\C++\dma\er_new\UnityExplorer-main\README.md
J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer.sln
J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\main.cpp
J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\MetickAdapter.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\GameOffsets.h
J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole.sln
J:\Code\C++\dma\er_new\UnityExplorer-main\App\ExternalResolveConsole\main.cpp
J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\os\dma\dma_memory_accessor.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\init\module_match.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\naraka\buff_manager_probe.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\naraka\runtime_property_probe.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\include\er2\unity2\naraka\actor_container_probe.hpp
J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Structure
J:\Code\C++\dma\er_new\UnityExplorer-main\Analysis\Algorithm
```

### YJWJ_DMA_NEW

```text
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\Offset.h
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\SDK.h
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\SDK.cpp
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\Thread.cpp
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\Hook.cpp
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Naraka\ESP.cpp
J:\Code\C++\dma\er_new\YJWJ_DMA_NEW\Release\dmaLog
```

## 8. 已知维护注意项

- [README.md](J:\Code\C++\dma\er_new\UnityExplorer-main\README.md) 提到 `BuildAndRun.bat`，但当前仓库根目录没有该文件。不要依赖该说明，使用对应 `.sln` 或 MSBuild/VS Build。
- [App\UnityExplorer\GameOffsets.h](J:\Code\C++\dma\er_new\UnityExplorer-main\App\UnityExplorer\GameOffsets.h) 存在过时字段和重复的 `PlayerName` 定义，分析时不要把它当作当前生产 SDK。
- `FindUnityRuntimeModules()` 选取第一个符合前缀规则的模块。若进程同时载入多个 `GameAssembly*.dll`，必须查看模块枚举日志并人工确认最终选中的 DLL 变体。
- 每次游戏更新、模块变体变化或换局后的空对象都应先重新做只读验证。RVA、class/static field 和对象链是不同层级的证据，不能只验证其中一层。

