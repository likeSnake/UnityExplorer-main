# 英雄目录与动作配置导出研究

- 日期：2026-09-12
- 目标：完整英雄目录的 ID、名称、分类与动作配置关联，不统计对局玩家。
- 输入：现有 SuperDumper 源码、2026-09-11 Super dump、`F:/gua/dump/dump_all` 的历史配置导出。
- 当前进程检查：2026-09-12 05:33 的系统信息发现 NarakaBladepoint.exe，PID 25208；WinAPI 模块枚举返回错误 5（拒绝访问），未获取当前模块身份。该 PID 是当时快照，不供后续直接复用。
- 成功条件：确认真实配置入口，区分表记录数、普通英雄候选数与当前开放数；为后续 dump 扩展提供可验证的字段及完整性要求。
- 约束：目标进程只读；不注入、不调用游戏方法、不改动游戏状态；历史参考不冒充当前运行态。
- 状态：文件导出 DLL 与独立加载测试已完成；游戏运行时读取和 SuperDumper 自动采集未完成。用户已授权后续构建和加载，不需要重复确认授权；本轮采用文件样本验证，不是游戏内验证。

成果入口：[研究报告与交接](report.md)、[工具使用说明](../../../App/HeroCatalog/README.md)。

## 目录

- `scripts/`：本次只读诊断与离线分析工具。
- `evidence/`：生成的诊断结果、源文件哈希。
- `artifacts/`：派生英雄清单与统计。
- `bundles/`：与 dump 分离的英雄、技能和动作资料目录；推荐消费这里的清单。
- `report.md`：分析结果与接入建议（分析完成后生成）。
