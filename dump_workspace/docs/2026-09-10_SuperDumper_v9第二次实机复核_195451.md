# SuperDumper v9 第二次实机复核

日期：2026-09-10。本轮任务为检查最新输出；没有修改 Dumper 源码或重新构建 DLL。

## 结论

本轮输出仍为 v9 的再次运行。文件生成、编码与索引一致性检查通过，但导出数量和诊断统计与 19:01 的上一轮相同，Protobuf 名称误过滤、接口数组布局疑点和 IDMAP 空字段数组均继续复现，不能判定这些问题已修复。

上一轮助手只记录了检查结果，没有交付下一版修复 DLL。本轮磁盘上的 DLL 与之前交付的 v9 哈希一致。运行日志的版本标记也为 v9；日志未记录已加载 DLL 的哈希，因此不将磁盘哈希冒充运行时加载文件的直接测量。

## 本轮文件与版本证据

- 正文：`F:\gua\UnityExplorer-main\dump_workspace\inject_test\out_super\dump_20260910_195451_711.cs`。
- 结构目录：同目录 `dump_20260910_195451_711_structures`。
- 日志：同目录 `log.txt`，首行 `SuperDumper v9 (接口校验、完整成员遍历与质量诊断)`。
- 模块：GameAssembly_Super.dll，基址 `0x7FF93DC00000`，PID 8496。
- 正文：83000398 字节，1987394 行。
- 正文 SHA256：`337B6218286DD02B3F559C3E1F75CBCC45DE447D40D22C887342B27B3D25F7B6`。
- 当前 `bin\Release\SuperDumper.dll`：529920 字节，修改时间 18:46:05。
- 当前 DLL SHA256：`349D56D9DF41ADE860AB65D6676C450368AC772BB89117D404DC03C3E47534A3`，与 v9 交付记录一致。

## 全量验证结果

复用上轮的只读核对脚本，对新的时间戳文件重新检查，没有复用旧输出作为本轮检查结果。

| 内容 | 本次结果 | 与上轮统计对比 |
| --- | ---: | --- |
| 类型 | 56495 | 相同 |
| 字段 | 297979 | 相同 |
| 方法 | 532497 | 相同 |
| 属性 | 82235 | 相同 |
| 接口关系 | 6913 | 相同 |
| 异常成员组 | 34559 | 相同 |
| 拒绝条目统计 | 335831 | 相同 |
| 数量上限跳过 | 0 | 相同 |
| 正文及正常索引转义非法字节 | 0 | 相同 |

正文、全部结构文件及日志均可严格按 UTF-8 读取。清单中的行数和字节数与文件一致，未发现 pending 残留。

全部 532497 个方法按类、VA、Slot 和完整签名与正文匹配；每类字段、方法、属性计数匹配。类型、接口、诊断和指针槽的 ClassId 关联通过，RVA 算术和方法排序通过，诊断分类计数自洽。

36738 个候选槽位覆盖 36671 个类型，19824 个类型未命中；65 个类型存在多个候选，正文选用的 8 个槽位关联正确。候选仍不等于已验证的业务基址。

机器检查结果：`dump_workspace\validation\v9-real-output-check\verification_195451_711.json`，`consistency_errors` 为空。与上一轮 `verification.json` 比较，除 dump 文件名和 SHA256 外，各项已采集统计均相同，正文大小和行数也相同。

当前目录已没有上一轮原始正文，本轮不宣称两个正文逐字节相同。

## 三项问题仍在

1. **Protobuf 名称误过滤。** 新诊断第 2415、2416 行仍显示 OpeningTimeResponse 的 `pb::Google.Protobuf.IMessage.get_Descriptor` 与 `pb::Google.Protobuf.IMessage.Descriptor` 被判为名称无效；全量方法 InvalidName 为 17079，属性为 5693，均与上轮相同。仍有一个带 GUID 花括号的 PrivateImplementationDetails 类型被名称规则跳过。

2. **接口数组布局或步长仍有疑点。** 6913 条已输出接口的 ArrayIndex 全为偶数。ActorModel 仍是原始数 14、尝试 14、输出 7，第一个失败索引 1 的所谓类地址为 `0x4`。接口数组偏移仍为 `0xB0`，计数仍为 `0x128`；扩展校准日志的样本、得分和“保留原偏移”结论与上轮相同。这继续支持“将接口与偏移成对的记录按单指针数组读取”的判断，尚未修正数组识别。

3. **IDMAP 字段数组仍为空。** 例如本轮诊断第 27192 行的 Assembly-CSharp / IFix.IDMAP0，RawCount=32760、Attempted=0、Exported=0、NullArray=32760；记录的数组槽地址是 `0x25F3CFA8090`。IDMAP 系列的空数组问题仍在，不能由重复运行推断其成员已恢复。

拒绝数量是当前原始读数和解析流程的统计，不等于已经证实存在同等数量的有效漏项。文件与索引一致也不证明接口布局和全部成员语义正确。

最终日志仍为 `result=OK_WITH_WARNINGS`。后续需要实际修改这些遗留问题并构建新版后，才能验证对应修复效果；本轮结果仅确认 v9 的重复运行表现。
