#pragma once

// Keep the protocol headers usable from any project that adds only `include`
// to its include path.  The repository already vendors nlohmann/json 3.11.3.
#include "../../deps/Naraka/json.hpp"

